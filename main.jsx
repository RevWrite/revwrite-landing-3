import React from "react";
import ReactDOM from "react-dom/client";
import RevWriteLanding from "./RevWriteLanding";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RevWriteLanding />
  </React.StrictMode>
);
